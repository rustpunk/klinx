# CLAUDE.md

Claude Code uses this file as its repository entry point.

@AGENTS.md

## Claude Code Notes

- Treat `AGENTS.md` as the shared contract for agent rules, repository layout, commands, and definition of done. Do not duplicate repository guidance here; update `AGENTS.md`, a local `AGENTS.md`, or `docs/ai` instead.
- Local `AGENTS.md` files load through a one-line `CLAUDE.md` beside them.
