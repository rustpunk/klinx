# CLAUDE.md

Claude Code uses this file as its repository entry point.

@AGENTS.md

## Claude Code Notes

- Treat `AGENTS.md` as the shared contract for agent rules, repository layout, commands, and definition of done.
- Read [`docs/ai/00_READ_THIS_FIRST.md`](docs/ai/00_READ_THIS_FIRST.md) next; the durable documentation set (architecture, invariants, testing, performance, glossary, open questions) lives in [`docs/ai/`](docs/ai/).
- Also read the nearest local `AGENTS.md` for the directory being edited, if one exists.
- Policies under `.claude/policies/` (architectural rigor, shortcut signatures) apply to sprint-close and PR audits.
- Do not duplicate repository guidance here. If shared guidance changes, update `AGENTS.md`, the relevant local `AGENTS.md`, or `docs/ai` instead.
