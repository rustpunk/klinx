//! Playwright MCP integration tests for composition drill-in on the Klinx canvas.
//!
//! These tests run against the web build of clinker-kiln (`dx serve --platform web`).
//! Desktop (wry) targets cannot be driven by Playwright.

#[test]
fn scaffold() {
    // Placeholder — the real canvas-composition tests run through Playwright.
}
